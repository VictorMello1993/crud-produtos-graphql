type Produto {
    id: Int,
    nome: String,
    preco: Float,
    desconto: Float,
    precoComDesconto: Float,
    categoria: String,
    disponivel: Boolean
}

input ProdutoFiltro {
    id: Int
    nome: String
}